# M7.5 T11/T12 gate brief — ready for founder

**When:** 2026-08-01  
**Block:** M7.5 Customer Readiness Regression  
**Eng:** GREEN (T01–T10 + ADV SC1–SC5 + residual R1/R2 SSRF)  
**Block close:** blocked on human gates only (D113)

## Pre-flight (this session)

| Check | Result |
|-------|--------|
| OSS current-state | HUMAN GATES OPEN |
| Cloud HEAD | `5731e5f` main |
| OSS HEAD | `40e7698` main (ahead origin; docs + T04/T07 CLI) |
| Verifier | PASS eng local — `docs/owa-records/m7.5-verifier.md` |
| Residual ADV | R1/R2 FIXED; 497/497 npm test |
| `https://cloud.agentpaas.ai` | **DOWN** (curl exit/HTTP 000) — post-M5 teardown still in effect |
| Brew CLI | `0.3.5` @ 977090a — **lags** main T04/T07 (`cloud result`/`logs`/inv_ store). T11 cold path should use a **main-built** binary or ship a release first. |

## What still open

### T11 — Cold-onboard (D113 non-deferrable)
Spec: founder + external tester, clean machine, **30 min**, runbook only  
Runbook: `docs/customer/golden-path.md`  
Pass bar: neither asks founder; agent writes CSV-ish output; 5m cron; completion webhook holds working signed URL; `cloud result` works.

### T12 — Live CF soak ($ cap)
Cron fires in prod; invoke E2E; artifact URL resolves; completion webhook delivers.  
Teardown scope if needed: **only** Worker `agentpaas-cloud-api` + its containers. **Never** `plain-cloud-0e69` / agentpaas.ai DNS.

## Recommended sequence

1. **Founder GO + $ spend cap** (required before any CF deploy).
2. **Bring API up always-on (T05 posture)** from cloud repo:
   - secrets/D1 already may exist from M4/M5; re-apply migrations if needed
   - `AGENTPAAS_PROD_COST_CAP_ACK=1` + registry ref + `npm run deploy:prod` (not bare wrangler)
   - rebind customer image after deploy
3. **Founder cold run of golden-path** (simulate clean shell: no history cheats).
4. **External tester** same runbook unaided (T11).
5. **Soak** cron + webhook + signed URL (T12) under the same $ cap.
6. Record pass/fail in owa-records; only then close M7.5.

## Known customer-readiness gaps (honest — may fail T11)

From verifier segment 7:

1. No OSS `provision-token` — operator `curl` + `X-Admin-Secret`.
2. Completion/delivery webhook + Cloud cron still **curl** (no CLI verbs) — M8 territory.
3. Push needs `CLOUDFLARE_API_TOKEN` (registry) separate from tenant token.
4. Brew CLI 0.3.5 lacks `cloud result`/`logs`/invoke-token store on main — **release or hand main binary before external T11**.
5. Whoami omits `preview` vault label (API has it; CLI residual WARN).

## Founder decisions needed now

Reply with:

A. **Spend cap $** for this live window (e.g. $5 / $10 / other)  
B. **Deploy GO** — bring `agentpaas-cloud-api` back (always-on per T05)? Y/N  
C. **T11 mode** — founder-only first, or founder + name external tester now?  
D. **CLI for T11** — (1) `go build` main binary for tester, or (2) cut brew/release first?  
E. **Demo agent** — weather-agent OK, or need a CSV-writing fixture as spec text says?

No CF deploy or secret reads until A+B are explicit.
