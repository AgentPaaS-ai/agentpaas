# OWA Record — B33-T08 Cross-container MCP reference proof (in progress)

**Branch:** feat/b33-t08-mcp-cross-container
**Date:** 2026-07-24
**HEAD:** 90cb341

## Done

| Chunk | Deliverable |
|-------|-------------|
| c1 | MCP fail-closed lifts when ServiceRegistry set (B33) |
| c2 | EnsureServices Declare+Start + daemon wrapper |
| c3 | MCP binding sidecar → harness ManagedServiceResolver |
| c4 | Both run paths: prepareMCPBindings + mount + finalize cleanup |
| c5a | HTTP endpoint `http://svc-<id>:8080`, DNS aliases, AttachClientContainer |
| c5b | Docker e2e `TestE2E_CrossContainer_LookupFeedback` **3× PASS** + `make mcp-container-e2e` |

## Local gate evidence (c5b)

```
AGENTPAAS_DOCKER_TESTS=1 go test ./internal/mcpmanager/ -run TestE2E_CrossContainer_LookupFeedback
  PASS ×3 (~27s each)
make mcp-container-e2e  PASS
go test ./internal/mcpmanager ./internal/daemon ./internal/runtime ./internal/harness -race  PASS
go build ./...  OK
```

E2E proves: service + client on internal network, capability header, distinctive `b33-t08-docker-e2e` marker, zero orphans after cleanup.

## Remaining (T08 exit gate)

- Full pack/CLI/API path with Hermes absent (signed packages) — optional stretch
- B26 admission matrix for MCP topology
- T09 block33-gate + adversary

## c5c evidence
Negative companion e2e suite PASS; see b33-t08-chunk5c.md.
Timeout/fence use service IP patch (real deadline/cancel, not host DNS miss).
