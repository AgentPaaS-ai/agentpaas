# M7.5 founder-run bugs — fix pass 2026-08-02

Inventory of issues uncovered during founder T11 / live invoke sessions
post M7.5 eng green, with fix status after this pass.

| ID | Severity | Symptom | Status |
|----|----------|---------|--------|
| BUG-deploy-invoke-deferStart | P0 | invoke stuck `starting` | FIXED earlier (cloud handleDeploymentInvoke starts + /invoke) |
| BUG-CF-BIND / UX-HTTP-BODY | P0 | deploy 503 only "unexpected status 503" | FIXED CLI: surfaces `error` body (`cf_bind_not_configured`); ops checklist still required |
| Flaky container start | P0 | intermittent exit 1 cold start | FIXED cloud 3f2503e startAndWaitForPorts + 5 retries |
| BUG-INSTANCE-SIZING | P0 | lite 256MiB OOM/exit1 ~20% | FIXED cloud default instance_type `dev` (bb46f82). Residual: pack-time measurement → lock resources (tracked) |
| ARM harness in amd64 image | P0 | CF exit 1 | FIXED OSS 35856a5 |
| UX-DLOG | P1 | Error: + error: double print | FIXED SilenceErrors on root |
| UX-DDOCKER | P1 | Docker: unknown stub | FIXED live docker probe |
| UX-APIHOST | P1 | blank whoami / DNS fail | FIXED wrapTransportError + golden-path API host section |
| UX-LOCKPATH | P1 | pack claims agent.lock in project | FIXED pack prints absolute Lock + push command |
| UX-DEPLOY | P1 | hand-copy digest only | FIXED `deploy latest` + `--lock` |
| UX-SECRETS-OUT | P2 | bare names | FIXED list header + count |
| UX-PUSH-OUT | P2 | quiet push | Already prints Image admitted + Digest (no change) |
| golden-path.md lies | P1 | wrong lock path, host, secret name | FIXED docs/customer/golden-path.md |

## Residual (not in this pass)

1. **BUG-INSTANCE-SIZING phase 2** — pack-time peak RSS → agent.lock resources → CF preset map (beyond default `dev`).
2. **cloud.agentpaas.ai DNS** — still needs zone; workers.dev + env is the supported path.
3. **Deployment secret bindings CLI** — still curl PUT (golden path documents it).
4. **Provision-token CLI** — still operator curl.
5. **wrangler deploy wipes customer image** — still need rebind after every deploy (ops).

## Evidence

- `go test ./internal/cloudclient/ ./internal/cli/` PASS
- Live soak post-dev instance: 20/20 invoke succeeded (prior session)
