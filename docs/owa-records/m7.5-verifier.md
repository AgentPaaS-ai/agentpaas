# M7.5 Verifier Report — Customer Readiness Regression (8-segment, pre-T11)

**When:** 2026-07-31T21:53:40Z  
**Role:** ap-verifier (READ ONLY product code)  
**Cloud HEAD:** `6e7622e` (`docs(m7.5): eng green; human gates T11/T12 open`) on branch `feat/m75-adv-residual`  
**OSS HEAD:** `e886db2` (`docs(m7.5): ADV SC1-SC5 DONE; eng green pending T11/T12`) on `main` (ahead 23 of origin)  
**CLOUD:** `/Users/pms88/projects/agentpaas-local/repos/agentpaas-cloud`  
**OSS:** `/Users/pms88/projects/agentpaas`

**Overall gate:** **VERIFY PASS (eng local)** — segments 1–7 PASS with disk evidence; segment 8 residual only (not claimed as PASS).  
**Honesty:** Does **not** close block. T11/T12 still OPEN. Does **not** authorize live CF spend. No live CF / real R2 prod URL / external cold tester claimed.

---

## 1. SPEC PRESENT — PASS

**Evidence**

- OSS `docs/execution/m7.5/block-spec.md` exists: T01–T12 work items + SC1–SC5 security claims; founder decisions **D112**, **D113**, **D114** referenced (header + sequencing §).
- OSS `docs/execution/m7.5/task-breakdown.md` table:
  - T01–T10 + ADV = **DONE**
  - T11 = **PENDING — D113 non-deferrable**
  - T12 = **PENDING**
- OSS `docs/execution/current-state.md`:
  - Title: `M7.5 ENG GREEN; HUMAN GATES OPEN`
  - T01–T10 + ADV SC1–SC5; Cloud ~480 tests
  - Block OPEN until T11 + T12
- Cloud `docs/execution/m7.5/*` present: T01–T05, T09 notes + `ADV-GAPS.md` (SC5 CLOSED).
- OWA ADV record: `docs/owa-records/m7.5-adv.md` — SC1–SC5 PASS eng; block NOT closed.

---

## 2. TESTS GREEN — PASS

**Commands (this run, disk logs)**

```text
cd CLOUD && npm test 2>&1 | tee /tmp/m75-ver-npm-test.log
# Test Files  34 passed (34)
#      Tests  480 passed (480)
# Duration  6.43s
NPM_EXIT=0

cd CLOUD && npm run typecheck 2>&1 | tee /tmp/m75-ver-typecheck.log
# tsc --noEmit
TYPECHECK_EXIT=0

cd OSS && env -u AGENTPAAS_CLOUD_TOKEN -u CLOUDFLARE_API_TOKEN -u CF_API_TOKEN \
  go test ./internal/cli/ ./internal/cloudclient/ -count=1 -timeout 180s \
  2>&1 | tee /tmp/m75-ver-go-cli.log
# ok  github.com/AgentPaaS-ai/agentpaas/internal/cli          4.110s
# ok  github.com/AgentPaaS-ai/agentpaas/internal/cloudclient  0.642s
GO_CLI_EXIT=0
```

**Counts:** cloud total=480, pass=480, fail=0  
**Note:** workerd noise `Containers have not been enabled for this Durable Object class` during RunContainer construction — suite still 480/480 PASS (same pattern as M5).  
**Not claimed:** full OSS `make test` / live CF deploy.

---

## 3. ADV PRESENT (SC1–SC5) — PASS (with QUALITY notes)

**File:** `test/m7.5-adv-sc.test.ts` (exists, 264 lines, 6 `it()`)

**Included in npm test green:**

```text
✓ test/m7.5-adv-sc.test.ts (6 tests) 577ms
```

**Grep / structure counts (file body)**

| Pattern | Hits / notes |
|---------|----------------|
| SC1 | 2 `it()` (rebind + expiry/tamper) |
| SC2 | 1 `it()` (HMAC unsigned/stale/replay via local helper) |
| SC3 | 1 `it()` (final_output only; forbidden sibling keys) |
| SC4 | 1 `it()` (flood → exact ceiling + typed 429) |
| SC5 | 1 `it()` (DELETE invoke-token + post-revoke 401) |
| 429 | present in SC4 |
| revoke | SC5 path DELETE + DB clear |
| HMAC / signPayload / verifyCompletionSignature | SC2 imports product sign/verify |
| final_output | SC3 asserts null + key set + no attacker strings |

**Assertion quality (not vacuous)**

- SC1: `expect(response.text()).toBe("tenant-a-bytes")`; expiry `token_expired` / `invalid_token` equality.
- SC3: key whitelist equality + `toThrow` on attacker promotion + serialized must-not-contain.
- SC4: admitted length 3, refused 17, each 429 + Retry-After + JSON `{error:"rate_limited", retry_after_sec}`.
- SC5: real DB null hash/prefix + soft expects 204/401/unauthorized/runCount 0 — **ran green** under 480 suite. Comment at L230 is historical BREAK text; ADV-GAPS.md marks SC5 **CLOSED**.

**ADV QUALITY gaps (not FAIL — residual honesty)**

1. **SC2 replay anti-duplication is implemented inside the test `receive` helper** (`seen` Set at L134–151), not proven as a product-side webhook receiver or platform replay store. Product still exposes real `signPayload` / `verifyCompletionSignature` (HMAC-SHA256). Unsigned/stale/wrong-ts signature failures use product verify.
2. SC5 uses `expect.soft` for HTTP status chain — weaker than hard fail-fast, but suite green.
3. No separate hollow `it()` without expect found in this file.

---

## 4. OUTPUT LOOP (T01–T03) — PASS

### T01 Artifacts + signed URLs

| Contract | Evidence |
|----------|----------|
| Tenant prefix | `src/artifacts.ts:1` `ARTIFACT_PREFIX = "tenants"`; `:67` key `tenants/{tenant}/runs/{run}/{name}` |
| Mint signed URL | `src/artifacts.ts:297-321` `mintArtifactURL` — payload `{t,r,n,exp}`, HMAC-SHA256, `?token=` |
| Verify + expiry | `src/artifacts.ts:324-369` `verifyArtifactToken`; L369 `payload.exp <= nowSec` → `{error:"token_expired", status:401}` |
| Unit tests | `test/m7.5-t01-artifacts.test.ts` — **9** `it()`, dense expects (not hollow) |

### T02 Completion webhook HMAC + SSRF

| Contract | Evidence |
|----------|----------|
| HMAC sign | `src/completion_webhook.ts:245-252` `signPayload` → `sha256=` HMAC hex over `` `${timestampSec}.${rawBody}` `` |
| Verify | `src/completion_webhook.ts:265-274` `verifyCompletionSignature` |
| SSRF config | `src/completion_webhook.ts:81` parse; `:132` configure; |
| SSRF delivery | `src/completion_webhook.ts:403` `assertPublicHttpsUrl` at deliver |
| Public IP helper | `src/ssrf.ts:63-170` RFC1918/loopback/link-local/metadata blocks; https-only |
| No redirects into private | `src/completion_webhook.ts:469` `redirect: "manual"`; `:476-477` reject 3xx |
| Unit tests | `test/m7.5-t02-completion-webhook.test.ts` — **12** `it()` |

### T03 Delivery channel declared final_output only

| Contract | Evidence |
|----------|----------|
| Payload shape | `src/delivery.ts:198-221` `kind: "final_output"` only declared fields |
| Reject promotion | `src/delivery.ts:224-264` `assertDeliveryPayloadSafe` forbidden key parts (`log`,`secret`,`event`,…) |
| SSRF config+delivery | `src/delivery.ts:77,128` config; `:376` delivery; `:446` redirect manual |
| Unit tests | `test/m7.5-t03-delivery.test.ts` — **8** `it()` |

---

## 5. RATE LIMIT + QUOTA UX (T08/T09 + SC4) — PASS

| Claim | Evidence |
|-------|----------|
| Defaults 60 invoke / 30 mgmt | `src/rate_limit.ts:1-2` `DEFAULT_INVOKE_RATE_LIMIT_PER_MIN = 60`, `DEFAULT_MGMT_RATE_LIMIT_PER_MIN = 30` |
| Applied on tenant create | `src/admin.ts:370-371` assigns those defaults |
| Sliding window DO/D1 counters | `src/rate_limit.ts:38-94` `checkAndIncrement` on `rate_limit_events` |
| Typed 429 + Retry-After | `src/rate_limit.ts:127-135` `rateLimitedResponse` status 429, header `Retry-After`, body `error:"rate_limited"` |
| Invoke path uses limits | `src/trigger_http.ts:136-155` → 429 + `retry_after_sec` |
| Mgmt path | `src/index.ts:1533-1537` `mgmt:${tenant_id}` |
| Quota messages actionable | `src/quota.ts:2-6` `contact@agentpaas.ai` / `https://agentpaas.ai` upgrade/extension |
| Monthly rollover | `src/usage.ts:40-64` UTC month start; `:68-90` CAS reset `cpu_minutes_used=0` |
| Rollover on admit + usage | `src/runs.ts:352+`; `src/usage.ts:121+` |
| Tests | `test/m7.5-t08-ratelimit.test.ts` (5 it); `test/quota-ux.test.ts` (5 it); ADV SC4 concurrent flood |

---

## 6. CLI SURFACE (T04/T07/T10) — OSS — PASS (T10 CLI label WARN residual)

**Help (live `go run ./cmd/agentpaas cloud --help`)**

- Subcommands present: `result`, `logs`, `invoke`, `invoke-token`, `usage`, `login`, `whoami`, `deploy`, `push`, `secrets`, …

**Wiring**

| Surface | Evidence |
|---------|----------|
| `cloud result` | `internal/cli/cloud_result.go:14-46` → `client.GetRunResult` |
| `cloud logs` | `internal/cli/cloud_result.go:78+` → `client.GetRunLogs` |
| cloudclient | `internal/cloudclient/run_result.go:38` GetRunResult; `:71` GetRunLogs |
| Invoke token store | `internal/cli/cloud_invoke_store.go:11` `invoke-tokens.json` under home; `internal/cloudclient/invoke_token_store.go:29+` FileInvokeTokenStore mode-0600 |
| Resolve order | `internal/cli/cloud.go:384+` flag → env → store (not flag/env-only) |
| Tenant token Keychain | `internal/cloudclient/keychain_token.go` KeychainTokenStore |
| go test | both packages EXIT 0 (this run); `cloud_result_test.go`, `invoke_token_store_test.go`, `cloud_invoke_test.go` present |

**T10 / D114 preview vault**

- Cloud API: `src/secrets/broker.ts:16` `SECRETS_BACKEND_LABEL = "preview"`; returned on whoami/secrets HTTP (`src/index.ts:237`, `src/secrets/http.ts:159`).
- **Residual WARN:** OSS CLI `whoami` **intentionally omits** secrets backend (`internal/cli/cloud.go:177-183`, print L222–223 only tenant/tier/concurrency). `ListSecretsResponse` does **not** decode `secrets_backend_label`; list prints names only (`cloud.go:950-951`). Spec asked preview in **docs and CLI output** — API label done; customer-visible CLI string incomplete.
- Admin insert still uses enum `secrets_backend = "shared_openbao"` (`src/admin.ts` create path) while customer label is `preview` — internal enum honesty residual (not marketed via whoami).

---

## 7. GOLDEN PATH TRUTH (T06) — PASS (customer-readiness gaps listed)

**Doc:** `docs/customer/golden-path.md` — single customer page (not owa-records dump).

**Covers (section map):**

1. Provision tenant token (operator curl)  
2. Login  
3. Pack  
4. Push  
5. Secrets  
6. Deploy  
7. Completion + delivery webhooks  
8. Invoke  
9. Cron  
10. Result / logs  
11. Usage  

**Spot-check vs real CLI help (3+ commands)**

| Doc command | CLI reality |
|-------------|-------------|
| `agentpaas cloud login` | present |
| `agentpaas cloud result <run_id>` | present (`cloud result --help`) |
| `agentpaas cloud logs <run_id>` | present |
| `agentpaas cloud invoke` / `invoke-token` / `usage` | present |

**Customer-readiness gaps (eng cannot PASS T11)**

- Doc admits OSS has **no** `provision-token`; uses admin `curl` + `X-Admin-Secret` (operator/founder step).
- Webhook config + Cloud cron still **curl** with tenant Bearer (no CLI verbs).
- Requires `CLOUDFLARE_API_TOKEN` for registry push (operator credential surface).
- Doc truthfully separates local `agentpaas cron add` from Cloud cron — good honesty.

**T11** is human validation of this doc — **not** claimed here.

---

## 8. GAPS (RESIDUAL — never PASS)

| Gap | Status |
|-----|--------|
| **T11** cold external tester (30 min, runbook only) | **PENDING** — D113 non-deferrable |
| **T12** live CF soak with $ cap | **PENDING** |
| Live CF webhook delivery / real R2 signed URL in prod | **RESIDUAL** — miniflare/unit only this run |
| Live cron fire in prod | **RESIDUAL** — unit/schema only |
| Deploy re-bind productized | **Eng docs/scripts:** `docs/execution/m7.5/T05-always-on.md`, `scripts/deploy-prod.sh` + `rebind-container-image.sh`; **not** live-executed by verifier |
| Always-on prod API posture | Documented only; no live probe |
| D114 CLI preview string | API label `preview`; CLI whoami/list do not print it |
| Tenant `secrets_backend` enum still `shared_openbao` at provision | Residual naming vs preview label |
| SC2 product-side replay store | Not required by claim “detectable”; ADV implements receiver-side set in test |
| Golden-path operator curls | Customer-readiness gap until CLI productizes webhook/cron/provision |
| ADV residual findings | `docs/execution/m7.5/ADV-GAPS.md` — SC5 CLOSED only; no open HIGH |
| Commercial “5 ACTIVE trial tenants” | Outside eng suite; not verified |
| Full OSS `make test` | Not run this session |
| Live spend / CF deploy | **NOT AUTHORIZED** by this report |

---

## Verdict matrix

| # | Segment | Result |
|---|---------|--------|
| 1 | SPEC PRESENT (T01–T12, SC1–SC5, D112–D114) | **PASS** |
| 2 | TESTS GREEN (480 + typecheck + go cli/cloudclient) | **PASS** |
| 3 | ADV PRESENT SC1–SC5 (6 it, in suite) | **PASS** (quality notes) |
| 4 | OUTPUT LOOP T01–T03 contracts | **PASS** |
| 5 | RATE LIMIT + QUOTA UX T08/T09 | **PASS** |
| 6 | CLI SURFACE T04/T07 (+ T10 API label) | **PASS** (T10 CLI WARN residual) |
| 7 | GOLDEN PATH T06 truth | **PASS** (curl gaps for T11) |
| 8 | GAPS | **RESIDUAL listed** (not PASS) |

---

## Final

**VERIFY PASS (eng local)**

- Does **not** close M7.5 block.  
- **T11 / T12 still open** (D113).  
- Does **not** authorize live CF spend or product close.  
- Orchestrator/human gates remain the only path to block close.

Logs: `/tmp/m75-ver-npm-test.log`, `/tmp/m75-ver-typecheck.log`, `/tmp/m75-ver-go-cli.log`

---

## Post-verifier residual ADV fix (same session)

After this verifier report, residual ADV found **R1/R2 SSRF hostname gaps**.
Fixed on cloud main:

- `0aaeb27` `fix(ssrf): block private-IP-embedding hostnames and loopback CNAMEs`
- `5731e5f` docs ADV residual closed
- Orch re-gate: **497/497 PASS**, typecheck 0 (`/tmp/m75-main-npm-clear2.log` → `/tmp/m75-main-npm-clean2.log`)

Verifier segments 1–7 remain PASS; segment 8 residual T11/T12 unchanged.
Eng clear for founder **T11** cold run (not a product close).

