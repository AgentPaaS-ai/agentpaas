# B31-T01 OWA Record

## Task: Registry Read API and Promoted Flag

## Worker: ap-worker (deepseek/deepseek-v4-pro @ nous)
## Date: 2026-07-20
## Duration: ~15 min, 183 messages, 179 tool calls

## What was implemented

1. **Promoted fields on InstallManifest** (internal/install/manifest.go):
   - `Promoted bool`, `PromotedAt *time.Time`, `PromotedBy string`
   - Default false via zero-value unmarshaling (no explicit migration code needed)

2. **DeclaredCapability type** (internal/pack/lock.go):
   - `DeclaredCapability` struct with ID and Description
   - `Capabilities []DeclaredCapability` added to AgentLock (additive, optional)
   - Parsed from agent.yaml via AgentYAML (internal/pack/detect.go)

3. **Registry read API** (internal/registry/registry.go — 391 lines):
   - `RegistryEntry` joined view: install manifest + deployment records + aliases + capability metadata
   - `ListEntries(stateRoot, store)` — bounded to MaxListEntries (100), deterministic sort (name asc, version asc)
   - `ShowEntry(stateRoot, ref, store)` — resolves by name@pub8, alias, or bare name (ambiguous → candidates list)
   - `DeploymentStoreReader` interface (subset of routedrun.Store)
   - Credential IDs only (no secret values exposed)

4. **CLI commands** (internal/cli/registry.go — 220 lines):
   - `agentpaas registry list [--json]` — tabular + JSON output
   - `agentpaas registry show <name|alias> [--json]` — full entry with capabilities
   - Registered in root.go

## Tests

- internal/registry/registry_test.go — 698 lines, 15 test functions:
  - Empty registry, installed-not-promoted, installed-promoted, migration (pre-registry reads as not promoted)
  - Deterministic ordering, credential IDs not values, digests match lock, deployment joined
  - ShowEntry by name, by alias, not-found, ambiguous name, deployment digests, bounded output, full entry
- internal/cli/registry_test.go — 301 lines, 7 CLI tests:
  - Human-readable list, JSON list (golden schema keys), human-readable show, JSON show
  - Ambiguous name error, not-found error, empty registry message

## Code review (orchestrator)

- All functions called — no dead code
- Credential values NOT exposed — only CredentialMap keys
- Promoted fields default false (zero-value migration)
- Deterministic ordering confirmed
- MaxListEntries bounds output (100)
- go build: 0 errors
- go test (all 47 packages): PASS
- golangci-lint (fresh cache): 0 issues
- Compat v0.2.3: PASS (no regression)

## Commits

- 0d794e2 B31-T01: add promoted fields to InstallManifest and capabilities to AgentYAML/AgentLock
- f434147 B31-T01: add registry read API (ListEntries, ShowEntry) with deterministic ordering
- cb47fa7 B31-T01: add CLI registry list and registry show commands

## Status: MERGED to main
