# B30-T00 — Close the synthetic MCP success fallback (fail-closed)

**Date:** 2026-07-19
**Branch:** feat/b30-t00-mcp-fail-closed
**Worker commit:** c4f2bab
**Merge commit:** feae6eb (main)
**Duration:** 12m 42s worker session (163 tool calls)

## Spec reference

`docs/execution/blocks/b30-summary.md:177-218` (T00). Pulled forward
from B33 per ap-thinker 2026-07-19 architecture audit, risk R5. Same
defect class as B20 C8 (fake LLM response).

## What changed

- `internal/harness/rpc_server.go` (`handleMCP`, lines 800-878): the
  no-router managed MCP path no longer returns a synthetic
  `{ok: true}`. In production it returns typed error code
  `agentpaas_mcp_service_not_enabled` (reserved by B26 in
  `internal/daemon/routed_handlers.go:562`), audits a `mcp_denied`
  record, and sets failure evidence. The synthetic result is retained
  only inside `AGENTPAAS_TEST_FAKE_MCP=1`, mirroring
  `AGENTPAAS_TEST_FAKE_LLM`. The router-installed branch and the
  undeclared-tool `mcp_denied` check are unchanged.

- `internal/harness/rpc_server_mcp_test.go` (new, 344 lines, 6 TDD
  tests):
  1. `TestMCP_NoRouter_FailsClosedInProduction` — production path
     returns typed error, no synthetic result, audit records denial.
  2. `TestMCP_NoRouter_FakeModeReturnsSynthetic` — explicit test mode
     returns synthetic `{ok: true}`, audit records call.
  3. `TestMCP_NoRouter_UndeclaredToolStillDenied` — allowlist check
     runs BEFORE fake path; undeclared tools denied in fake mode too.
  4. `TestMCP_WithRouter_FailsClosedDoesNotAffectRealRouter` — real
     router path unchanged; stub HTTP MCP server returns router data,
     not synthetic.
  5. `TestMCP_SyntheticPayloadStringAbsentInProduction` — source/AST
     guard: synthetic construction appears only after the
     `AGENTPAAS_TEST_FAKE_MCP` check inside `handleMCP`.
  6. `TestMCP_NoRouter_AuditRecordIsDeniedNotCall` — focused audit
     test: production path records exactly 1 `mcp_denied` event with
     typed reason.

- `internal/harness/sdk_contract_test.go`: added
  `t.Setenv("AGENTPAAS_TEST_FAKE_MCP", "1")` to
  `TestSDKMCPAllowlistAndDeniedAudit` — declared-server success case
  exercises the now-gated synthetic path.

- `internal/harness/failure_context_test.go`: added same env var to
  `TestFailureContextMCPHashesBodiesOnly` — MCP body-hash audit
  assertion needs the synthetic path.

- `docs/known-limitations.md`: updated MCP router limitation to
  reflect fail-closed behavior.

## Orchestrator code review (step 1.5)

PASS. All 6 new tests are real assertions (no `t.Log`-only docs, no
inverted conditions). Existing tests got env var added, not
weakened. Allowlist check correctly runs before fake-mode gate
(security: prevents test-mode from bypassing allowlist).

## Verification gate (orchestrator, post-merge)

- `go build ./...`: PASS (0 errors)
- `go test ./... -count=1`: ALL PASS — harness, daemon, mcpmanager,
  routedrun, runtime, trigger, secrets, service, trust, pack, policy,
  port/conformance, test/compat/v0.2.3, test/golden, test/redteam.
  No regressions.
- `go vet ./...`: clean
- `golangci-lint run --timeout 5m ./internal/harness/...`: 0 issues
  (fresh cache)
- `go test ./test/compat/v0.2.3 -count=1`: PASS (backward compat)

## Adversary (deferred to block-end per local-first OWA flow)

T00 is small and self-contained. Adversary will be invoked at block-end
as part of the T09 cumulative adversary matrix.

## Architecture review (deferred to block-end)

Per `agentpaas-owa-build-orchestration` skill, the architecture review
runs once per block after the adversary pass. T00 changes are isolated
to the harness MCP handler; no cross-block contract impact (B26
reservation honored).

## Acceptance criteria (exit gate, b30-summary.md:217-218)

> No governed MCP call can return a synthetic success in production.
> Small, self-contained diff; no dependency on B28 ports or B30
> runtime work.

PASS. Production path returns typed `agentpaas_mcp_service_not_enabled`.
Synthetic only behind `AGENTPAAS_TEST_FAKE_MCP=1`. No B28/B30 runtime
dependency introduced.

## Files changed

- `internal/harness/rpc_server.go` (+49 -0)
- `internal/harness/rpc_server_mcp_test.go` (+344, new)
- `internal/harness/sdk_contract_test.go` (+4 -0)
- `internal/harness/failure_context_test.go` (+4 -0)
- `docs/known-limitations.md` (+10 -3)

## Worktree cleanup

- `git worktree remove --force /Users/pms88/projects/ap-b30-t00`
- `git branch -D feat/b30-t00-mcp-fail-closed`
- `git worktree prune`

## Next task unblocked

T01 — Convert B26 characterization into mandatory regression tests.
Depends on T00 (per spec task order). Ready to dispatch.
