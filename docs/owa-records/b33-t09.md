# OWA Record — B33-T09 Block gate + adversary matrix

**Branch:** feat/b33-t09-block-gate  
**Date:** 2026-07-24  
**HEAD:** see git log

## Delivered

### Makefile
- `make block33-gate` — full chain: block32-gate + B33 packages + Python SDK + mcp-container-e2e + adversary + vet + lint + govulncheck + golden-fast
- `make block33-gate-fast` — same without block32 chain
- `make restart-local-daemon` — restart agentpaasd from `./bin` after build-all (fixes stale-daemon pack signing)

### Adversary matrix (`adversary_b33_matrix_test.go`)
14-row inventory + gap regressions (worker endpoint ignored, stale cap, no caller secrets, managed path not hardcoded 5s).

### Pack lock signature fix
- `agentYAMLCanonicalMap` snake_case omitempty in lockCanonicalMap
- AgentYAML json tags for ReadAgentLock
- G47 / golden-fast requires **running daemon = just-built binary**

## Local evidence
```
go test mcpmanager/harness/daemon/runtime/routedrun/policy/pack -race PASS
python SDK 145 OK
TestAdversary_B33* PASS
mcp-container-e2e PASS
lock Verify after pack PASS (snake_case agent_yaml)
bundle inspect lock_provenance PASS
golden-fast G47 PASS (with restart-local-daemon)
```

## Operator note
After `make build` / `build-all`, always `make restart-local-daemon` (or golden-fast does it). Overwriting bin/agentpaasd while old process runs leaves pack RPC on stale code → lock_provenance FAIL.

## Next
Merge T09 → B34 unblocked. Full `make block33-gate` includes long block32 chain.
