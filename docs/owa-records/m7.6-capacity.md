# M7.6 — Capacity invariant + run-admission re-rule (D123/D124)

**Status:** ENG GREEN + LIVE SMOKE PASS (2026-08-03)
**Cloud main:** 507a89d (FF merge feat/m7.6-capacity)
**OSS main:** 8e0c230 + CLI already_running mapping (FF merge feat/m7.6-cli-already-running)
**Worker:** agentpaas-cloud-api version eafa54f6… live
**Tests:** cloud `npm test` 553/553 PASS

## Delivered

| Item | Evidence |
|------|----------|
| Slot pool 100 | mig 0021 applied remote; D1 COUNT(slots)=100, free≈95 post-undeploy |
| max_instances 25 | wrangler.toml + deploy diff EDIT max_instances 5→25 |
| trial concurrency 10 | CONCURRENCY_MAP trial:10; founder tenant patched 5→10 |
| already_running | admitRun NOT EXISTS active run per deployment; 429 |
| Capacity invariant | assertCapacityAllows on provision/patch |
| max_run_lease_min | tenants column default 30 |
| Zombie reaper | reapExpiredRuns on scheduled; crons `*/5` + `0 3 * * *` |
| Start failure | 503 container_start_failed; run deleted (not customer failed) |
| CLI mapping | `agent already running; retry in Ns` |

## Live smoke (orch, 2026-08-03)

1. D1 mig 0021 applied remote ✅
2. deploy-prod + rebind customer weather image ✅ (max_instances 25)
3. cloud deploy latest → dep_f90608f5 ready slot-11 ✅
4. secrets push/bind openrouter-key ✅
5. invoke Folsom → succeeded real 110°F ✅
6. concurrent double-invoke: one succeeded, other **rc=1** `agent already running; retry in 30s` ✅
7. undeploy → slot freed ✅

## Manual gate remainder (founder, optional D113)

Spec gate also wants: second trial tenant multi-deploy concurrent runs, lease=1 kill, admin capacity refuse with low PLATFORM_MAX_INSTANCES.
Unit tests cover capacity refuse + reaper; live multi-tenant + lease kill not re-run this session (cost). Recommend founder spot-check or accept eng+double-invoke smoke.

## Next

M8 residual close (signed-URL delete regression card if not already proven) → M8.6 → M8.5 → M9.
