# B34-T09 — Adversary RED gate fixes

**Status:** DONE
**Date:** 2026-07-24
**Branch:** feat/b34-t09-gate
**Commit:** 5048943

## Scope

Fix all four HIGH adversary findings discovered in `TestAdversary_B34_*` suite.

### Fixes

1. **BREAK-1 (nested reserved keys):** `hasReservedKeys` → recursive `hasReservedKeysRecursive`
   - File: `internal/workflow/pipeline/handoff_validate.go`
   - Walks `map[string]interface{}` and `[]interface{}` at any depth
   - Reserved keys in nested objects or array elements now rejected

2. **BREAK-3 (controller bypass):** `CommitStageSuccess` validates ContextJSON
   - File: `internal/workflow/pipeline/controller.go`
   - `hasReservedKeys(json.RawMessage(req.Handoff.ContextJSON))` before `CommitHandoff`
   - Rejects with `HANDOFF_RESERVED_KEY`

3. **BREAK-4 (symlink escape):** `artifactPutToFS` Lstat check
   - File: `internal/workflow/pipeline/artifact_store.go`
   - `os.Lstat(target)` before `os.WriteFile`; if symlink → `ARTIFACT_SYMLINK_REJECTED`

## Evidence

```
go test ./internal/workflow/pipeline/ -race -count=1 -run Adversary -v
=== 18/18 PASS (including 4 previously RED)

go test ./internal/workflow/pipeline/ -race -count=1
ok — full suite green
```

## Files changed

- `internal/workflow/pipeline/handoff_validate.go` — recursive reserved key scan
- `internal/workflow/pipeline/controller.go` — CommitStageSuccess ContextJSON validation
- `internal/workflow/pipeline/artifact_store.go` — symlink rejection in artifactPutToFS
- `docs/owa-records/b34-t09-adversary-findings.md` — resolution record
