# B30-T09 — OWA Record

## Task
Block gate and adversary review

## Date
2026-07-20

## Commits
- `d1014eb` B30-T09: adversary break tests for durable runtime (14 tests, grok-4.5)
- `ec6f4a2` B30-T09 fix: verify checkpoint digest on resume (adversary break fix, deepseek-v4-pro)
- `99fd1bd` B30-T09: block30-gate Makefile target + block28-ci/block28-long stubs
- Merge: `B30-T09: block30-gate Makefile target + adversary matrix + digest verification fix`

## Files created/modified (6 files, +925 lines)
- `internal/supervisor/adversary_b30_test.go` (705 lines) — 14 adversary break tests
- `internal/routedrun/progress.go` (+43 lines) — ComputeDigest and VerifyDigest methods on SemanticCheckpoint
- `internal/routedrun/resume_test.go` (modified) — updated to use proper digest computation
- `internal/routedrun/adversary_b27_test.go` (modified) — updated for digest verification
- `internal/supervisor/fault_injection_test.go` (modified) — updated for digest verification
- `Makefile` (+42 lines) — block30-gate, block28-ci, block28-long targets

## Adversary tests (14, all pass)
1. TestAdversary_B30_ReintroduceHiddenTimeout — TimeEnvelope is authoritative
2. TestAdversary_B30_ClientQueryTimeoutCancelsWorker — query timeout doesn't cancel run
3. TestAdversary_B30_ForgeJobAcceptedEvent — forged journal event rejected by HMAC
4. TestAdversary_B30_DeliverJobTwice — duplicate result rejected (idempotent)
5. TestAdversary_B30_ReuseKeyWithChangedInput — same key, different input = CONFLICT
6. TestAdversary_B30_InvokeInactiveDeployment — inactive deployment rejected
7. TestAdversary_B30_StdoutSpamPreventsStall — stdout spam doesn't prevent stall
8. TestAdversary_B30_LateSuccessAfterCancellation — late result after cancel rejected
9. TestAdversary_B30_DaemonRestartBlindReplay — no blind replay on restart
10. TestAdversary_B30_ArtifactPathTraversal — path traversal rejected
11. TestAdversary_B30_CheckpointDigestTamper — tampered checkpoint rejected (FIXED)
12. TestAdversary_B30_ChargePausedWallTime — PAUSED zero, PAUSE_REQUESTED charged
13. TestAdversary_B30_FrozenStateWithLiveCapability — frozen state has no live capability
14. TestAdversary_B30_ProcessAliveNotProgress — process existence is not progress

## Adversary break found and fixed
- **CheckpointDigestTamper**: The supervisor read checkpoints from disk without verifying their digest. A tampered checkpoint was accepted as safe-to-resume. FIX: Added `ComputeDigest()` and `VerifyDigest()` methods to `SemanticCheckpoint` in `internal/routedrun/progress.go`. The supervisor now verifies the digest on read and rejects mismatches.

## block30-gate target
```make
block30-gate: block29-gate block28-ci
    - supervisor + routedrun + harness + daemon tests with race
    - runtime + trigger + operator + CLI tests with race
    - adversary tests (TestAdversary_B30)
    - compat v0.2.3 regression
    - Python SDK tests
    - vet + lint (fresh cache)
    - golden-fast
```

## Verification
- go build ./... — PASS
- go test ./... -count=1 — ALL PASS (including compat v0.2.3)
- go test ./internal/supervisor/... -count=1 -race — ALL PASS
- golangci-lint run ./internal/supervisor/... ./internal/routedrun/... — 0 issues

## Compatibility impact
- `progress.go`: additive (new methods, existing behavior unchanged for valid digests)
- `resume_test.go` and `adversary_b27_test.go`: updated to use proper digest computation (test-only changes)
- `Makefile`: additive (new targets)
- compat v0.2.3 tests pass

## Next task unblocked
Architecture review (ap-thinker) and verifier pass.
