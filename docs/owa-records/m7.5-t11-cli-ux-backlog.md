# M7.5 T11 — CLI UX backlog capture (not in-gate)

**When:** 2026-08-01 founder cold-path setup  
**Action:** recorded only — no eng work until after T11/T12 (or polish wave)

1. **Double error log** — `Error:` + `error:` on same failure (identity exists, daemon already running).
2. **Docker unknown in daemon status** — stub fields; doctor is source of truth today.

Canonical list: `docs/execution/post-m5-backlog.md` → “CLI UX polish”.
