# B31-T02 OWA Record

## Task: Promotion Operation and Delegation Gate

## Worker: ap-worker (deepseek/deepseek-v4-pro @ nous)
## Date: 2026-07-20
## Duration: ~11 min, 157 messages, 153 tool calls

## What was implemented

1. **Promote/Demote operations** (internal/registry/promote.go - 165 lines):
   - `Promote(stateRoot, ref, actor)` - sets Promoted=true, PromotedAt, PromotedBy
   - `Demote(stateRoot, ref)` - clears promoted fields
   - Both idempotent (already-promoted/demoted = no-op success)
   - Both resolve ref via install.ResolveAgentRef (name@pub8 or alias)
   - Both emit audit events with fingerprint, digest, actor
   - saveManifest uses 0600 permissions

2. **CLI commands** (internal/cli/registry.go +80 lines):
   - `agentpaas registry promote <ref>` 
   - `agentpaas registry demote <ref>`
   - 4 new CLI tests

3. **Workflow promotion validation** (internal/registry/workflow_validate.go - 102 lines):
   - `ValidateWorkflowPromotedPackages(stateRoot, wf)` checks all named packages
   - Checks service bindings, pipeline stages, child allowlist
   - Returns actionable error: "promote it first: agentpaas registry promote <name>"
   - Graceful degradation: empty stateRoot skips check
   - Only applies to installed packages (uninstalled = skip)

4. **Audit event types** (internal/audit/event_types.go):
   - `EventTypePackagePromoted = "package_promoted"`
   - `EventTypePackageDemoted = "package_demoted"`

## Tests

- internal/registry/promote_test.go - 386 lines, 9 tests:
  - Promote sets flag, idempotent, emits audit, by alias, unknown ref, full cycle
  - Demote clears flag, idempotent, emits audit
- internal/registry/workflow_validate_test.go - 270 lines, 8 tests:
  - Nil workflow, empty stateRoot, service binding, child allowlist
  - Unpromoted package fails, promoted passes, demote-then-fail, locally owned package
- internal/cli/registry_test.go - +115 lines, 4 new CLI tests

## Code review (orchestrator)

- All functions called - no dead code
- Promote/Demote idempotent (already-promoted = no-op)
- Audit events include fingerprint, digest, actor
- No credential values exposed
- saveManifest uses 0600 permissions
- go build: 0 errors
- go test (all packages): PASS
- golangci-lint (fresh cache): 0 issues
- Compat v0.2.3: PASS

## Commits

- 468463b B31-T02: add Promote/Demote operations with audit events
- e9c69d4 B31-T02: add CLI promote and demote commands
- aecae9e B31-T02: add workflow promotion validation (registry.ValidateWorkflowPromotedPackages)

## Status: MERGED to main
