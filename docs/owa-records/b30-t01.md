# B30-T01 — Convert B26 characterization into mandatory regression tests

**Date:** 2026-07-19
**Branch:** feat/b30-t01-characterization
**Worker commit:** 27c57a1
**Merge commit:** d6da9a7 (main)
**Duration:** 10m 54s worker session (93 tool calls)

## Spec reference

`docs/execution/blocks/b30-summary.md:221-258` (T01). Goal: turn every
current accidental lifetime limit into an executable requirement.

## What changed

- `test/compat/v0.2.3/b30_t01_durable_ceilings_test.go` (new, 325
  lines, 10 tests): replaces the 4 t.Log-only characterization tests
  with executable regression tests.

- `test/compat/v0.2.3/v023_test.go`: 4 superseded t.Log-only tests
  deleted (DaemonInvokeTimeout, HarnessBudgetDefault,
  ModelClientTimeout, RLimits). Other characterization tests not about
  the 7 ceilings are preserved.

## The 7 ceilings registered (each with owning future task)

| # | Ceiling | Source | Owner |
|---|---|---|---|
| 1 | Daemon 2-minute context | internal/daemon/control_handlers.go:909 | B30-T03 |
| 2 | Inner urlopen(timeout=60) | internal/daemon/control_handlers.go:1500 | B30-T02 |
| 3 | Harness 5-minute default (300s) | cmd/harness/main.go:24 | B30-T03 |
| 4 | Harness 120s budget | internal/harness/budget.go:17 | B30-T03 |
| 5 | Model client 120s HTTP timeout | internal/harness/rpc_server.go:471 | B30-T03 |
| 6 | Python RLIMIT_CPU=30 | internal/harness/python_worker.go:477 | B30-T04 |
| 7 | Python RLIMIT_NPROC=0 | internal/harness/python_worker.go:479 | B30-T04 |

## TDD pattern

Tests 1-7 use the "fail then pass" pattern:
- Each asserts the CURRENT (broken) behavior on the baseline today
  (strings.Contains on source bytes — passes on baseline).
- Then `t.Skip` with the owning future task name so the gate stays
  green.
- When the owning task replaces the fixed timeout with a policy-derived
  value, the corresponding test gets UNSKIPPED and the assertion is
  INVERTED to verify the new behavior.

Test 8 (LegacyPathConstantsAllowlisted) registers the requirement that
any durable-path fixed timeout either be documented as legacy/compat
OR replaced. Skipped on baseline.

Test 9 (FixedTimeoutOwnershipScanner) runs WITHOUT skip on every gate:
scans the 7 source locations and asserts each ceiling literal is STILL
PRESENT. Regression guard against silent removal before replacement.
Also asserts the registry has exactly 7 entries (guards against
accidental shrinkage).

Test 10 (LegacyCompatPathUnchanged) runs WITHOUT skip: smoke test that
the legacy v0.2.3 invoke path still works (mockPayloadBuilder).

## Orchestrator code review (step 1.5)

PASS. Tests 1-7 use real `mustContain` assertions before the Skip (so
the Skip is informed by an actual source check, not a bare skip).
Tests 9 and 10 run without skip and use `t.Errorf`/`t.Fatalf` with real
assertions. The `b30T01Ceilings` registry is checked for length 7 in
test 9 (guards against accidental shrinkage).

## Verification gate (orchestrator, post-merge)

- `go build ./...`: PASS (0 errors)
- `go test ./... -count=1`: ALL PASS — harness, daemon, mcpmanager,
  routedrun, runtime, trigger, secrets, service, trust, pack, policy,
  port/conformance, test/compat/v0.2.3, test/golden, test/redteam.
  No regressions.
- `go vet ./...`: clean
- `golangci-lint run --timeout 5m ./test/compat/... ./internal/harness/...`: 0 issues
- `go test ./test/compat/... -count=1 -race`: PASS (1.4s)

## Adversary (deferred to block-end)

T01 is test-only (no production code changes). Adversary will be
invoked at block-end as part of the T09 cumulative adversary matrix.

## Architecture review (deferred to block-end)

Per `agentpaas-owa-build-orchestration` skill, the architecture review
runs once per block after the adversary pass. T01 changes are test-
only; no cross-block contract impact.

## Acceptance criteria (exit gate, b30-summary.md:255-258)

> Every observed v0.2.3 limit has a test that fails for the intended
> reason on the baseline and cannot be "fixed" by changing only a
> comment.

PASS. All 7 ceilings have executable tests with real assertions. The
7 skipped tests carry owning-task messages (T02/T03/T04). Test 9
(regression guard) and test 10 (legacy compat) run without skip on
every gate.

## Files changed

- `test/compat/v0.2.3/b30_t01_durable_ceilings_test.go` (+325, new)
- `test/compat/v0.2.3/v023_test.go` (-66)

## Worktree cleanup

- `git worktree remove --force /Users/pms88/projects/ap-b30-t01`
- `git branch -D feat/b30-t01-characterization`
- `git worktree prune`

## Next task unblocked

T02 — Implement direct durable asynchronous deployment invocation/job
protocol. Depends on T01 (per spec task order).
