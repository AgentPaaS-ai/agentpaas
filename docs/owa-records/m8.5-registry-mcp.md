# M8.5 — Platform registry + BYO MCP hosting

**Status:** ENG GREEN + LIVE API 2026-08-03
**Cloud main:** 2f3b55d (mig 0022 + GET /v1/registry + kind)
**OSS main:** FF feat/m8.5-registry-cli
**Worker:** redeployed with mig 0022 applied remote
**Tests:** cloud m8.5-registry suite in merge; OSS go test cli+cloudclient PASS

## Delivered

| Item | Evidence |
|------|----------|
| GET /v1/registry tenant-scoped | src/registry.ts + tests isolation |
| secrets labels only | m8.5-registry.test.ts |
| deploy kind agent\|mcp | deployments.ts + CLI --type |
| platform.mcp_catalog stub | registry response structure |
| CLI cloud registry | internal/cli + --json |
| Hermes agentpaas_cloud_registry | tools/schemas/plugin.yaml |

## Residuals (honest)

- Full B33 cross-container MCP DNS mesh on CF not proven live (MCP kind deploys as container with same egress story; agent→MCP call path needs follow-up soak)
- Platform MCP catalog is structural stub (empty/public list), not populated dogfood MCPs
- 1 flaky artifact token test seen once in suite re-run (unrelated); full suite was green on worker

## Manual gate

Founder: deploy --type mcp trivial server, registry show, agent tool call — deferred to founder session (cost).

## Next

M9 audit + metrics APIs
