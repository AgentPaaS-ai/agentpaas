# B30-T06 — OWA Record

## Task
Build the multi-turn long-running reference worker (research dossier)

## Date
2026-07-20

## Commits
- `d16ffc0` B30-T06: reference worker -- deterministic 21-turn research dossier with fake model/tool responses, checkpoint/artifact persistence, and resume support
- Merge: `B30-T06: 20-turn multi-turn reference worker (research dossier)`

## Worker model
deepseek/deepseek-v4-pro @ nous

## Files created (2 files, +1184 lines)
- `internal/supervisor/reference_worker.go` (716 lines) — deterministic 21-turn reference worker
- `internal/supervisor/reference_worker_test.go` (468 lines) — 10 tests (all pass with -race)

## Tests written (10 tests, all pass with -race)
1. TestReferenceWorker_ExactTurnCount — exactly 21 turns (20 work + 1 finalize)
2. TestReferenceWorker_LaterRequestContainsPriorDigest — turn 6+ prompt contains checkpoint digest from turn 5
3. TestReferenceWorker_ArtifactDependency — turn 11+ depends on artifact from turn 5
4. TestReferenceWorker_ProgressSequence — strictly increasing sequence numbers, no gaps
5. TestReferenceWorker_CheckpointSequence — 4 checkpoints + 1 final, each with valid digest
6. TestReferenceWorker_ArtifactDigestsStable — running twice produces identical digests
7. TestReferenceWorker_NoRawPromptsInAudit — audit log contains no raw model prompts/responses
8. TestReferenceWorker_ResultNotCalledVerified — final result does not claim "verified" or "correct"
9. TestReferenceWorker_ContextBound — worker does not exceed configured context bound
10. TestReferenceWorker_ResumeSkipsCommitted — resume from checkpoint skips committed turns

## Spec coverage (T06 required work, lines 488-512)
- [x] 1. Read a bounded fixture set
- [x] 2. At least 20 governed turns across model and tool phases
- [x] 3. Explicitly carry bounded prior-turn transcript/state into later model prompts
- [x] 4. Persist intermediate structured state to artifacts
- [x] 5. Emit heartbeat progress during legitimate slow phases
- [x] 6. Commit safe checkpoint after each material phase
- [x] 7. At least one later phase depends on earlier artifact digest
- [x] 8. Return final structured result and artifact reference
- [x] 9. On resume, skip already committed actions
- [x] 10. Deterministic fake model/tool responses, no LLM judge

## Verification
- go build ./... — PASS
- go test ./... -count=1 — ALL PASS (including compat v0.2.3)
- go test ./internal/supervisor/... -count=1 -race — ALL 25 PASS (15 T05 + 10 T06)
- go vet ./... — clean
- golangci-lint run ./internal/supervisor/... — 0 issues
- No em-dash in Go string literals

## Compatibility impact
- Additive: new files only, no existing APIs changed
- No backward compat regressions

## Next task unblocked
T07 (restart/cancel/active-time/cleanup races) — can now exercise the reference worker under fault injection.
