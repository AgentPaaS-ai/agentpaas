# ORCH PROMPT — Pre-nuclear E2E bar (M8+ + open bugs)

**Audience:** ap-orchestrate (this profile). Load this file and execute until the bar is green.  
**User goal:** Founder can run **full nuclear teardown** then **end-to-end golden path** (terminal and/or Hermes) without ops heroics.  
**Created:** 2026-08-03  
**Repos:** OSS `~/projects/agentpaas` main; cloud `~/projects/agentpaas-local/repos/agentpaas-cloud` main.  
**Canonical bug list:** `docs/owa-records/OPEN-BUGS-post-m7.5.md`  
**Do not bump version past 0.3.6 unless user says so** (rebuild same tag OK).

---

## Mission (one sentence)

Close every **pre-nuclear P0/P1** below, complete **M8 (rescoped)** so run-record CLI is solid, then **you** (orch) prove nuclear → brew → local weather → cloud pack/push/deploy/bind/invoke/result on disk before asking founder to try.

---

## Hard rules

1. Orch never writes product code if OWA worker is preferred — but user may allow orch quick fixes; follow session preference.
2. Never fabricate agent/cloud output. Disk evidence only.
3. Never nuclear on founder machine without saying so; when proving nuclear, restore brew/daemon and prove doctor 7/7 after.
4. Prove live cloud with `AGENTPAAS_CLOUD_API_URL` (workers.dev until DNS). Do not require founder homework until orch green.
5. `apc_…` = tenant token; `CLOUDFLARE_API_TOKEN` = registry — never conflate.
6. CI is local only. Push main when green.
7. English, terse status reports.

---

## PART A — Bugs that MUST be fixed before nuclear E2E (not optional)

Track in OPEN-BUGS. Fix in order.

### A1. P0 — Undeploy + slot release (BLOCKER)

- [ ] Cloud API: `DELETE /v1/deployments/:id` (tenant auth) → mark/remove deployment → `releaseSlot`
- [ ] Idempotent cross-tenant refuse
- [ ] OSS CLI: `agentpaas cloud undeploy <dep_id>`
- [ ] Docs: golden-path step “free capacity”
- [ ] Ops: one-shot free orphan slots (slot-1 orphan, other-tenant junk as needed)
- [ ] Test: deploy → undeploy → slot free → deploy again succeeds
- Doc: `docs/owa-records/BUG-undeploy-slot-release.md`

### A2. P0 — Slot model matches trial story (BLOCKER for multi-agent trial)

- [ ] Product decision locked: trial always-on deploys = N slots (recommend **5** to match concurrency, or document deploy-on-invoke)
- [ ] Implement per-tenant slot quota OR enlarge pool and isolate tenants so one trial isn’t blocked by another
- [ ] Customer-visible error when at **their** limit (not opaque shared pool only)
- [ ] Evidence: trial tenant can hold agreed N concurrent deployments

### A3. P1 — Instance sizing phase 2 (heavy agents)

- [ ] Default `dev` remains floor (phase1 done on cloud main)
- [ ] Pack or deploy path records resources; bind maps to CF preset
- [ ] No silent lite 256MiB after wrangler/rebind
- Residual R2 / ADV-GAPS BUG-INSTANCE-SIZING phase2

### A4. P1 — Post-wrangler image rebind automated

- [ ] `deploy-prod` / documented path always rebinds customer image after `wrangler deploy`
- [ ] Founder nuclear path cannot leave platform Dockerfile as runtime image
- Residual R3

### A5. P1 — Worker secrets / CF bind checklist

- [ ] After any Worker recreate: ADMIN, SECRETS_MASTER_KEY, CF_API_TOKEN, CF_ACCOUNT_ID, CF_CONTAINER_APP_ID, CF_EGRESS_ALLOWLIST proven
- [ ] Optional: `cloud doctor` or deploy error already surfaces `cf_bind_not_configured` (CLI body done)
- BUG-cf-bind PARTIAL → close when checklist is mechanical

### A6. P1 — API hostname

- [ ] Either `cloud.agentpaas.ai` DNS live **or** brew/CLI default points at live workers.dev with clear message
- Residual R4

### A7. P1 — Orch-proven paths before founder nuclear

- [ ] Terminal: `docs/customer/golden-path.md` green after undeploy exists (full deploy not only fallback)
- [ ] Hermes: `docs/customer/golden-loop-hermes-e2e.md` phases 1–6 disk-proven once (profile teardown OK; full machine nuclear optional second pass)
- [ ] Evidence files under `docs/owa-records/golden-*-YYYYMMDD.md`

---

## PART B — M8+ that must be complete (rescoped truth)

### B1. M8 — Run-record persistence CLI (depends on M7.5 eng)

M8 is **not** artifacts/webhooks (those are M7.5). M8 remainder:

- [ ] Full run-record retained until **explicit tenant deletion** (D84) — implement/verify API + storage policy
- [ ] CLI solid: `cloud status` / runs list, `cloud logs <run>`, `cloud result <run>` on brew bar
- [ ] Delete-run or tenant delete clears records + artifact URLs die (founder gate intent)
- [ ] Tests + local CI green; docs honest
- Spec pointer: `docs/execution/planning/managed-cloud-m-series-plan.md` M8 rescoped section  
  (if gitignored locally, use cloud/OSS owa + this prompt)

### B2. M8.5 — Only if nuclear E2E claims “registry/MCP stranger path”

Skip for **minimum** nuclear weather golden unless founder expands scope.

- [ ] Platform registry + BYO MCP hosting (D78) — only if E2E script includes MCP

### B3. M9+ — Not required for nuclear weather E2E

Audit/metrics APIs, Stripe, dashboard — **out of scope** for this bar.

---

## PART C — Nuclear E2E definition of done (founder may run only after orch GO)

Orch runs this first. Founder repeats only on GO.

### C0. Preconditions orch proves

- [ ] A1–A6 closed or explicitly waived by founder in writing
- [ ] M8 B1 closed or waived
- [ ] ≥1 free slot (or undeploy works)
- [ ] Live API URL set; whoami works
- [ ] Brew 0.3.6+ (or current agreed tag) doctor 7/7

### C1. Nuclear (machine)

```text
# Only with founder OK — wipes brew agentpaas, colima/docker state, ~/.agentpaas, AgentPaaS keychain labels
# Use skill nuclear-teardown script path from agentpaas-manual-testing / local-ci notes
```

- [ ] Post-nuclear: reinstall colima/docker, brew agentpaas, xattr -cr, daemon start, doctor 7/7
- [ ] identity init; secret add openrouter-key (user stdin)
- [ ] CLOUDFLARE_API_TOKEN + AGENTPAAS_CLOUD_API_URL + open tenant claim link, then cloud login (or --token-stdin with provider token)

### C2. Local agent

- [ ] Pack weather (demo or built) linux/amd64
- [ ] Local run + invoke OK; invoke-response on disk
- [ ] Optional: egress deny then allow (Hermes loop)

### C3. Cloud

- [ ] push --lock absolute
- [ ] deploy succeeds (not only no_slot fallback)
- [ ] secrets push + bind
- [ ] invoke-token; invoke; result final_output real weather
- [ ] undeploy frees slot; second deploy works
- [ ] usage shows trial meters

### C4. Evidence

- [ ] `docs/owa-records/nuclear-e2e-YYYYMMDD.md` with run_ids, dep_ids, GO/NO-GO
- [ ] Only then: hand founder the same runbook

---

## PART D — Execution order for orch

1. Read OPEN-BUGS-post-m7.5.md + this prompt.  
2. Implement **A1 undeploy** end-to-end (cloud + CLI + tests).  
3. Slot quota/pool decision **A2** + implement minimum.  
4. A3–A6 as needed for reliability.  
5. Finish **M8 B1** run-record deletion/retention if not already true.  
6. Merge cloud+OSS main; brew rebuild **same 0.3.6** or user-approved tag.  
7. Orch runs **C1–C4** (nuclear only after A1 works).  
8. Report GO/NO-GO with file paths. Founder nuclear only on GO.

---

## PART E — Explicit non-goals (do not boil ocean)

- Website Worker plain-cloud-0e69  
- Stripe / M6 full billing  
- K8s M11/M12  
- Changing trial pricing  
- Waiting on GitHub Actions  

---

## Success line (copy when done)

```text
NUCLEAR_E2E_BAR=GO
undeploy=yes slot_free_proven=yes m8_run_record=yes
orch_evidence=docs/owa-records/nuclear-e2e-YYYYMMDD.md
founder_may_run_nuclear=yes
```

## Failure line

```text
NUCLEAR_E2E_BAR=NO-GO
blocking=<P0-id list>
do_not_ask_founder_nuclear=yes
```

---

## Quick refs

| Path | Why |
|------|-----|
| docs/owa-records/OPEN-BUGS-post-m7.5.md | Bug checklist |
| docs/owa-records/BUG-undeploy-slot-release.md | Undeploy bug |
| docs/customer/golden-path.md | Terminal short path |
| docs/customer/golden-loop-hermes-e2e.md | Hermes E2E |
| docs/owa-records/m7.5-pre-m8-residuals.md | Residuals R1–R9 |
| Cloud main | container start + instance_type dev already merged |

END OF PROMPT
