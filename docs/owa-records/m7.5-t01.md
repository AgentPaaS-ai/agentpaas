# M7.5-T01 DONE — Artifacts + signed R2 URLs (SC1)

**Date:** 2026-07-31
**Cloud main:** 557e462 (merge)
**Branch tip:** bc9390b
**Gate:** npm test 442 PASS on main

## Shipped
- R2 binding ARTIFACTS (`wrangler.toml`)
- `src/artifacts.ts` — put/get/list, assembleResultPackage, HMAC signed URLs
- Migration 0012 `run_artifacts`
- Routes: POST `/v1/runs/:id/artifacts`, GET `/v1/runs/:id/result`, GET `/v1/artifacts/get?token=`
- Terminal path assembles result package (best-effort)
- Tests: `test/m7.5-t01-artifacts.test.ts`

## SC1 enforcement (file:line)
- Token binds tenant+run+name+exp: `src/artifacts.ts:312-320` mint, `:367-371` verify
- Tamper invalidates HMAC: constant-time compare `:288-294`, `:368`
- Fetch loads only key from verified claims: `src/index.ts` artifacts get handler
- Cross-tenant API 404 on result/post: tests + getRun tenant scope
- Name sanitize rejects `../`: `sanitizeArtifactName` `:41-51`

## Notes
- Worker pushed feature branch + attempted PR (killed); local-first merge only.
- Container miniflare noise pre-existing; tests still green.

## Next
T02 completion webhook HMAC + SSRF
