# v0.3.6 same-tag rebuild — 2026-08-03 (founder E2E prep)

Tag: v0.3.6 @ `952296d` (was f4c9135; retagged, no version bump)
Reason: brew bar must carry the cloud invoke Run ID fix before the founder
drives a cold tenant E2E from brew install.

## What changed since f4c9135

- `df5b09c` fix(cli): cloud invoke prints run id and final output
  (deploy-invoke wire shape is `{run_id, status, final_output}` — was being
  decoded as a run record, so Run ID printed empty and final output was dropped)
- `455a23b` fix(ops): nuclear teardown keeps founder CF API token
  (cherry-picked user correction d253cc5 back onto main; HEAD had regressed)
- `952296d` docs(customer): golden-path tenant-token only; drop stale CF
  export + dev instance hint

## Release mechanics (local CI, no GH Actions)

1. Focused tests green: `go test ./internal/cli/ ./internal/daemon/
   ./internal/cloudclient/ -count=1` — all ok
2. Retag: `git tag -a v0.3.6` at main; `gh release delete v0.3.6 --cleanup-tag`
   then recreate (asset names already existed from prior 0.3.6)
3. Park untracked (`??` owa-records) → `goreleaser release --clean --skip=sign`
   → restore
4. Cask pushed to AgentPaaS-ai/homebrew-tap (new amd64/arm64 SHAs)
5. In-repo `Formula/agentpaas.rb` mirror updated (378908c)
6. `brew uninstall --cask agentpaas && brew install --cask agentpaas` — OK
7. `xattr -cr` on the four binaries (Gatekeeper quarantine)

## Verify

- `agentpaas version` → `CLI: 0.3.6 | Commit: 952296dd8d4a75767201b29d00080de849073bc8`
- Release assets: agentpaas_0.3.6_darwin_{amd64,arm64}.tar.gz (+sbom), checksums.txt
- Live cloud invoke now prints `Run ID: run_…` + `Final output:` (proven earlier
  with df5b09c built from source; brew bar matches)

## Open before founder E2E

- Cloud Worker live at d24e91a0 (upload fixes: 384MiB total cap, per-tenant
  registry namespace, 24h reaper cron `0 3 * * *`); migration 0020 applied
- Fresh trial tenant provisioned: ten_7cbb53735fd95aa8e000d29146839533
  (token at /tmp/e2e-tenant-token.txt, 600) — re-provision if stale
- Nuclear teardown keeps CF token (455a23b)
