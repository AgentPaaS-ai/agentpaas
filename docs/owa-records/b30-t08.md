# B30-T08 — OWA Record

## Task
Run the longevity proof matrix

## Date
2026-07-20

## Commits
- `c0494ad` B30-T08: longevity proof matrix -- fake-clock 24h/100-turn test, parameterized turn count, real-time stubs
- Merge: `B30-T08: longevity proof matrix (fake-clock 24h/100-turn + real-time stubs)`

## Worker model
deepseek/deepseek-v4-pro @ nous

## Files created/modified (3 files, +519 lines)
- `internal/supervisor/longevity_test.go` (290 lines) — fake-clock 24h test + real-time test stubs
- `internal/supervisor/reference_worker.go` (modified, +221 lines) — parameterized turn count, phase configuration
- `internal/supervisor/reference_worker_test.go` (modified, +8 lines) — updated for configurable turns

## Tests written
1. **TestLongevity_FakeClock24Hour_100Turns** (PASS, 0.37s):
   - 100 turns over simulated 24 hours
   - 21 checkpoints committed
   - 122 progress events, strictly increasing, no gaps
   - Active time consumed: 30000 ms (correctly excludes PAUSED/NEEDS_REPLAN intervals)
   - No overflow, no duplicate finalization
   - Context bound: 17046/65536 (within limits)
   - Terminal state: SUCCEEDED
   - Result stored with 101 turns

2. **TestLongevity_RealTime6Min_20Turns** (stub, requires AGENTPAAS_DOCKER_TESTS=1):
   - Real daemon + Docker topology
   - 6 minutes, 20 turns, crosses 60s/120s/300s boundaries
   - Client disconnect/reconnect

3. **TestLongevity_RealTimeSoak30Min_100Turns** (stub, requires AGENTPAAS_DOCKER_TESTS=1):
   - 30 minutes, 100 turns, 10+ checkpoints
   - Daemon restart after checkpoint
   - Resource sampling (memory, PIDs, FDs, journal size)

## Spec coverage (T08, lines 559-608)
- [x] Fake-clock 24-hour run: 100 turns, regular progress/checkpoints
- [x] Model/tool delays, lease-expiry simulation, active-time boundary
- [x] Long PAUSED/NEEDS_REPLAN intervals consume zero active time
- [x] PAUSE_REQUESTED consumes time
- [x] No overflow, timer leak, unbounded growth, or duplicate finalization
- [ ] Real-time proof A (6 min, 20 turns): stubbed, requires Docker
- [ ] Real-time proof B (30 min, 100 turns, soak): stubbed, requires Docker
- [x] Real-time tests require AGENTPAAS_DOCKER_TESTS=1 (no skip in CI)

## Verification
- go build ./... — PASS
- go test ./... -count=1 — ALL PASS (including compat v0.2.3)
- go test ./internal/supervisor/... -count=1 -race — ALL PASS
- golangci-lint run ./internal/supervisor/... — 0 issues

## Notes
- The real-time proofs (A and B) are stubbed with proper skip conditions. They require Docker and the real daemon. These will be run as part of the block-end verification or R30 prerelease with Docker enabled.
- The fake-clock proof is comprehensive: 100 turns, 24h simulated, all assertions pass.
- The reference worker was extended to support configurable turn counts and phase configuration without breaking existing tests.

## Compatibility impact
- `reference_worker.go` modified (additive: new options, existing defaults preserved)
- No backward compat regressions (compat v0.2.3 tests pass, existing reference worker tests pass)

## Next task unblocked
T09 (block30-gate Makefile target + adversary matrix).
