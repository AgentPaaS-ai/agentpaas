# B30-T04 — Replace hidden Python rlimits with policy-derived container limits

**Date:** 2026-07-19
**Branch:** feat/b30-t04-rlimits
**Worker commit:** b600e2f (worker hit 90-iter budget; orchestrator applied em-dash syntax fix)
**Merge commit:** 3a3fbeb (main)
**Duration:** 27m 8s worker session (203 tool calls)

## Spec reference

`docs/execution/blocks/b30-summary.md:395-430` (T04). Owns ceilings 6
(RLIMIT_CPU=30) and 7 (RLIMIT_NPROC=0) from the T01 registry.

## What changed (13 files, +982 -24)

### Durable path: policy-derived limits
- `internal/harness/python_worker.go` (+116): `apply_resource_limits`
  now conditional. On durable path (env `AGENTPAAS_DURABLE_CPU_QUOTA_S`
  or `AGENTPAAS_DURABLE_MAX_PIDS` set), uses policy values. On legacy
  path, keeps RLIMIT_CPU=30 / RLIMIT_NPROC=0 with "legacy compat" comments.
- `internal/daemon/b30_t04_durable_resources.go` (+74, new): wires
  durable-path env vars from the invocation payload into the container
  spec.
- `internal/runtime/driver.go` (+7): ContainerSpec gains MaxPIDs,
  CPUQuotaSeconds, MemoryLimitBytes fields.
- `internal/runtime/docker.go` (+11): Docker driver honors PidsLimit
  (default 256 when 0).
- `internal/harness/server.go` (+28): Config carries policy resource
  limits.
- `internal/harness/failure_context.go` (+31): resource-limit
  termination reason recorded via `formatResourceLimitDetail`.
- `internal/operator/schema.go` (+7): policy schema gains cpu_quota_seconds,
  max_pids, memory_limit_mb.
- `internal/routedrun/types.go` (+19): AttemptReport gains CPUSeconds
  field (separate from AttemptDurationMs).
- `cmd/harness/main.go` (+31): harness reads durable env vars.

### T01 ceiling tests unskipped and inverted
- `test/compat/v0.2.3/b30_t01_durable_ceilings_test.go` (+62 -?):
  `TestB30T01_PythonRLimitCPU30_Failing` and
  `TestB30T01_PythonRLimitNPROC0_Failing` unskipped. Assertions
  inverted: durable path NO LONGER has the fixed rlimits; legacy path
  keeps them documented as "legacy compat". Registry updated (2 T04
  entries removed).

### 20 tests across 4 new test files
- `internal/harness/b30_t04_rlimits_test.go` (+434, 13 tests):
  PythonRunnerDurablePathNoFixedRLimitCPU, PythonRunnerDurablePathNoFixedRLimitNPROC,
  PythonRunnerDefaultMaxPIDsSufficientForLocalTools, ConfigCarriesPolicyResourceLimits,
  WorkerEnvPropagatesPolicyLimits, CPUBoundWorkBeyond30SecondsSucceeds,
  LegacyPath_StillUsesRLimitCPU30_NPROC0, ExplicitCPUBudgetTerminates,
  AllowedBoundedSubprocessSucceeds, ForkBombContained,
  ResourceLimitTerminationReasonRecorded, ChildAgentCreationNotOSExec.
- `internal/runtime/b30_t04_resources_test.go` (+95, 3 tests):
  PidsLimitDefault, PidsLimitPolicyOverride, ContainerSpecCarriesPolicyResourceFields.
- `internal/daemon/b30_t04_durable_resources_test.go` (+91, 3 tests):
  DurablePathResourceEnv, LegacyRunPathDoesNotSetDurableEnv, ResourceLimitDetailFormat.
- Extra test in harness (1): WorkerEnvPropagatesPolicyLimits.

## Worker iteration budget

Worker hit 90/90 iteration budget at 27m 8s (203 tool calls). The
implementation + 20 tests + T01 test inversions were complete but
uncommitted. Worker's final message recommended committing as-is and
fixing the 2 t.Fatalf em-dash syntax errors. Orchestrator applied the
em-dash fix (mechanical test-only syntax fix per pitfall #129) and
committed.

## Orchestrator code review

PASS. Durable/legacy split is correct: durable path uses policy env
vars, legacy path keeps fixed rlimits with "legacy compat" comments
(T01 allowlist satisfied). ContainerSpec extension is additive (new
fields, no breaking changes). Docker driver PidsLimit default 256 is
sufficient for local tools (git, grep, awk) but contains fork bombs.
Resource-limit termination reason recorded via formatResourceLimitDetail.

## Verification gate (orchestrator, post-merge)

- `go build ./...`: PASS (0 errors)
- `go test ./... -count=1`: ALL PASS — daemon 65.0s, routedrun 15.1s,
  harness 19.4s, compat v0.2.3, golden, redteam. No FAIL. No regressions.
- `go vet ./...`: clean
- `golangci-lint run --timeout 5m`: 0 issues (fresh cache)

## Acceptance criteria (exit gate, b30-summary.md:426-429)

> No hidden worker-bootstrap rlimit ends a valid durable run; every
> resource stop maps to explicit policy evidence.

PASS. RLIMIT_CPU=30 and RLIMIT_NPROC=0 removed from durable path
(policy-derived). Legacy path keeps them documented. Resource stops
record typed reason + observed value in attempt evidence.

## Worktree cleanup

- `git worktree remove --force /Users/pms88/projects/ap-b30-t04`
- `git branch -D feat/b30-t04-rlimits`
- `git worktree prune`

## Next task unblocked

T05 — Durable supervisor + liveness reconciliation. T05 owns ceiling 2
(inner urlopen timeout=60) — it replaces the lifetime-spanning urlopen
with the durable InvokeJob protocol (the harness consumes one job
envelope exactly once). T05 also owns the corrupt-JSON-validation gap
deferred from T02 Part B.
