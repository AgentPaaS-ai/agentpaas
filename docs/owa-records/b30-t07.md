# B30-T07 — OWA Record

## Task
Prove restart, cancellation, active-time boundaries, and cleanup

## Date
2026-07-20

## Commits
- `81bf2e2` B30-T07: fault injection tests -- restart, cancel, lease, digest tamper, idempotent reconcile/cleanup
- Merge: `B30-T07: restart/cancel/active-time/cleanup fault injection tests`

## Worker model
deepseek/deepseek-v4-pro @ nous

## Files created (1 file, +1393 lines)
- `internal/supervisor/fault_injection_test.go` (1393 lines) — 25+ fault injection tests

## Tests written (all pass with -race)
1. TestFault_RestartDuringIdleBetweenTurns — restart during idle, verify resumable
2. TestFault_RestartDuringModelCall — restart during model call, lease revoked
3. TestFault_RestartDuringToolPhase — restart during tool phase, lease revoked
4. TestFault_RestartDuringCheckpointCommit — restart mid-checkpoint, partial state handled
5. TestFault_RestartDuringFinalResultCommit — restart mid-result, no false success
6. TestCancel_DuringIdleBetweenTurns — cancel during idle, CANCELLED, no further progress
7. TestCancel_DuringModelCall — cancel during model call, late model end rejected
8. TestCancel_DuringCheckpointCommit — cancel during checkpoint, checkpoint preserved or not
9. TestCancel_DuringFinalResultCommit — cancel + late result at commit boundary, cancel wins
10. TestLeaseExpiry_AfterClaim — lease expiry revokes, no further progress accepted
11. TestStallTimer_FiresAfterTimeout — stall detected after timeout with no progress
12. TestActiveTimeExhaustion — active time past max, run EXPIRED
13. TestLateSuccessAfterCancellationCannotWin — late result rejected after CANCELLED
14. TestCheckpointDigestTamper — tampered checkpoint detected on reconcile
15. TestResultDigestTamper — tampered result detected on reconcile
16. TestReconcile_TwiceIsIdempotent — double reconcile, no duplicate events
17. TestCleanup_TwiceIsIdempotent — double cleanup, no errors
18. TestContainerExit_ZeroWithoutResultNotSuccess — exit 0 without result NOT SUCCEEDED
19. TestRuntimeStopFailure_CleanupGraceful — cleanup handles runtime stop failure
20-25. Additional tests: SecondDaemonRestartNoDuplicateCleanup, JournalIntegrityAfterRestart, CheckpointOrderingPreserved, LateLeaseMismatchRejected, etc.

## Spec coverage (T07 required work, lines 527-551)
- [x] 1. Inject daemon restart during idle/model-call/tool-phase/checkpoint/result-commit
- [x] 2. Inject harness/worker exit and container runtime errors
- [x] 3. Cancel during the same phases
- [x] 4. Expire lease, stall timer, and max active duration independently
- [x] 5. Reconcile control/progress journals and artifacts after each fault
- [x] 6. Verify no blind replay, false success, stale lease activity, or lost terminal result
- [x] 7. Assert cleanup of containers, networks, control material, journal keys, temp creds
- [x] 8. Repeat reconciliation twice to prove idempotency

## Exit gate
Every fault yields one honest terminal or resumable state and zero accepted governed actions after lease revocation. CONFIRMED by tests.

## Verification
- go build ./... — PASS
- go test ./... -count=1 — ALL PASS (including compat v0.2.3)
- go test ./internal/supervisor/... -count=1 -race — ALL PASS
- go vet ./... — clean
- golangci-lint run ./internal/supervisor/... — 0 issues
- No em-dash in Go string literals

## Compatibility impact
- Additive: new test file only, no production code changed

## Next task unblocked
T08 (longevity matrices) and T09 (block30-gate Makefile target).
