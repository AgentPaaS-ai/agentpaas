# B29-T01: Streaming/Lifecycle Characterization Tests

**Status:** COMPLETE
**Date:** 2026-07-19
**Worker:** ap-worker (deepseek/deepseek-v4-pro, 12m 42s, 181 tool calls)
**Base:** 836ca7a (B28 complete)
**Merge commit:** 755eb9d

## Task

Freeze the current (pre-B29) streaming/lifecycle behavior in characterization
tests so that B29's replacements (T02-T07) are detectable as test deltas.

## What was characterized (5 observations)

1. **agent.llm() is buffered** (Python SDK, `test_b29_t01_characterization.py`)
   - 8 tests. Asserts exactly one `_call("llm", params)` RPC, single dict
     return, no `llm_stream` method exists, no `messages` multi-role field.

2. **InvokeStream is synthetic** (`b29_t01_invokestream_characterization_test.go`)
   - 5 tests. Asserts InvokeStream does NOT call production admission path.
     Run goes Created → Succeeded immediately with no harness RPC, no agent
     container, no model invocation. Exactly 2 events (RunCreated, RunSucceeded).

3. **EventBus is in-memory, bounded, drops, non-restartable**
   (`b29_t01_eventbus_characterization_test.go`)
   - 6 tests. Asserts in-memory map, channel buffer capacity 64, silent event
     drops when full, new EventBus loses all state, terminal closes all
     subscriber channels.

4. **Event subscription not the durable source of truth**
   (`b29_t01_event_vs_checkpoint_test.go`)
   - 4 tests. Asserts B27 progress journal survives restart, EventBus events
     do not. Journal is the durable source of truth, not EventBus.

5. **Cold-per-run lifecycle, no warm pool**
   (`b29_t01_lifecycle_characterization_test.go`)
   - 6 tests. Asserts fresh runID per invocation, no warm pool, no activation
     policy on TriggerService, ActivationPolicy exists only in port package.

## Orchestrator code review

- 5 files, 1033 lines, zero production code modified ✓
- All 29 tests pass (21 Go + 8 Python) ✓
- `go build ./...` clean ✓
- `go test ./... -count=1` all packages green ✓
- `golangci-lint` 0 issues (fresh cache) ✓
- EOF newlines: one missing (fixed, amended) ✓
- No uncalled functions ✓
- Tests are real characterization tests with proper assertions ✓
- Proper B29-T01 CHARACTERIZATION TEST headers ✓

## Issues found

1. **Worker created PR #183 on GitHub** — VIOLATION of local-first mode
   (OWA skill: "NO PR, NO push" per subtask). PR closed by orchestrator.
   Root cause: worker prompt did not explicitly forbid PR creation. Fix:
   future T01 prompts will include "Do NOT create a GitHub PR, do NOT push
   to origin, commit locally only."

## Adversary

Skipped for T01 — characterization tests have no security surface (they test
existing behavior, not new code). Adversary runs on implementation tasks
(T02-T07).

## Verifier

Deferred to block-end (B29-T08).

## Next

B29-T02: Runtime profile and normalized envelopes.
