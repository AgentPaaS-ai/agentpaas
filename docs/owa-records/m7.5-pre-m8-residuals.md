# Pre-M8 residuals (todos)

Captured 2026-08-02 after founder T11 + live invoke debug.
If a residual is **not** in M8+ scope, fix it **before** starting M8.

M8 scope (rescoped): run-record retention until tenant delete; polish on
`cloud runs` / `logs` / `result` only. Output loop already in M7.5.

## Must fix before M8 (customer stranger bar)

| ID | Residual | Owner path | Status |
|----|----------|------------|--------|
| R1 | Deployment secret bind still curl-only | OSS CLI `cloud secrets bind` | FIXED this pass |
| R2 | BUG-INSTANCE-SIZING phase2 (pack measure → lock resources → preset) | OSS pack + cloud bind | OPEN — default `dev` holds weather; complex agents need phase2 |
| R3 | wrangler deploy wipes customer image; rebind required | cloud scripts + deploy-prod | OPEN — document + automate in deploy-prod |
| R4 | cloud.agentpaas.ai DNS dead | founder CF zone | OPEN — workers.dev + AGENTPAAS_CLOUD_API_URL until live |
| R5 | Cloud branch `fix/retry-flaky-container-start` not on cloud main | merge + wrangler deploy | FIXED 2026-08-03 — FF merge to cloud main `5d0dc1b` |
| R6 | golden-path / golden-loop writeups stale (v0.2.3 local-only) | docs | FIXED this pass (rewrite) |
| R7 | Hermes E2E golden missing cloud + egress fail/fix | docs/execution + script | FIXED this pass (new loop) |
| R8 | CLI provision-token missing | ops curl OK for trial | DEFER — operator-only is intentional until self-serve M10 |
| R9 | **No undeploy / slot never freed** → no_slot_capacity stuck | Cloud DELETE dep + CLI `cloud undeploy` + releaseSlot | OPEN P0 — see BUG-undeploy-slot-release.md; **not** M8+ |

## Covered by M8+ (do not block M8 start)

| ID | Item | Milestone |
|----|------|-----------|
| M8-a | Full run-record retention until explicit delete | M8 |
| M8-b | `cloud runs` list polish if needed | M8 |
| M8.5 | Platform registry + BYO MCP | M8.5 |
| M9 | Audit/metrics APIs | M9 |

## Done earlier (do not reopen)

- deferStart invoke starts container
- container startAndWaitForPorts + retries
- instance_type default `dev`
- amd64 harness pack
- UX-DLOG / DDOCKER / APIHOST / HTTP-BODY / LOCKPATH / DEPLOY latest
