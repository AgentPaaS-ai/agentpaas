# B34-T08 — Operator inspection + pipeline enable flag + reference proof

**Status:** DONE
**Date:** 2026-07-24
**Branch:** feat/b34-t08-operator
**PRE:** e9165150d945325e418c1863ccebdbc012dff2d4
**Commits:**
  bf2d8ef feat(b34-t08): add PipelineInspectSummary and BuildPipelineInspect
  d4661a8 feat(b34-t08): add pipeline runtime enable flag
  2ed3021 test(b34-t08): add reference proof — three-stage Seed→Reconcile→Success

## Scope implemented

1. **PipelineInspectSummary** — bounded view of workflow + ordered nodes + outcomes + handoff digests. No secret content exposed.
2. **Enable pipeline admission** via `pipelineEnabled bool` on controlServer and `AGENTPAAS_PIPELINE_ENABLED=1` env var. Default (unset) still denies. Block code updated B30→B34.
3. **Reference proof test** — three-stage Seed→Reconcile→Success loop proving "Hermes-absent" library path (no Docker required). Documents snapshot/workflow IDs, verifies no duplicate launch keys.
4. **Makefile block34-gate** — runs pipeline package race + key package tests + symbol grep + doc check.
5. **Optional thin CLI format helper** — not included (deferred to T09 if needed).

## Evidence

### Pipeline inspect tests
```
go test -race -count=1 -run 'TestBuildPipelineInspect' ./internal/workflow/pipeline/
=== PASS: TestBuildPipelineInspect_Success (0.00s)
=== PASS: TestBuildPipelineInspect_MidFailure (0.00s)
```

### Pipeline enable tests
```
go test -race -count=1 -run 'Pipeline|NotEnabled|pipeline' ./internal/daemon/
ok — all existing not_enabled tests green (env unset); TestPipelineEnabled_AllowsDeployCheck passes both paths
```

### Reference proof
```
go test -race -count=1 -run 'TestReferenceProof_ThreeStageLinear' ./internal/workflow/pipeline/ -v
PASS: workflow=wf-... nodes=3 all SUCCEEDED, handoffs=2, launch keys=3 (no duplicates)
```

### Makefile gate
```
make block34-gate
Block 34 gate: PASS
```

## Files changed
- `internal/workflow/pipeline/inspect.go` — PipelineInspectSummary + BuildPipelineInspect
- `internal/workflow/pipeline/inspect_test.go` — success + mid-failure tests
- `internal/workflow/pipeline/reference_proof_test.go` — three-stage library proof
- `internal/daemon/stub_handlers.go` — pipelineEnabled field
- `internal/daemon/routed_handlers.go` — pipelineRuntimeEnabled(), B30→B34, enable gate
- `internal/daemon/routed_handlers_test.go` — B30→B34 assertion fix + TestPipelineEnabled_AllowsDeployCheck
- `Makefile` — block34-gate target + help text
- `docs/owa-records/b34-t08.md` — this record
- `docs/execution/current-state.md` — updated

## Known gaps / deferred
- Live Docker three-container e2e → T09
- Hermes pack live → T09
- Full B26 admission matrix for pipeline → T09
- Thin CLI format helper for inspect summary JSON/text → deferred

## Next
B34-T09: block34-gate adversary review + Docker e2e + Hermes pack live
