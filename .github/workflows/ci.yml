name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - name: Install golangci-lint
        run: brew install golangci-lint
      - name: Run lint
        run: make lint

  test:
    name: Test
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - name: Run tests
        run: make test

  race:
    name: Race Detector
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - name: Run race detector
        run: make race

  proto:
    name: Proto Codegen Drift
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - name: Install buf
        run: brew install buf protoc-gen-go protoc-gen-go-grpc protoc-gen-grpc-gateway
      - name: Regenerate proto code
        run: make proto
      - name: Check for drift
        run: |
          if ! git diff --exit-code; then
            echo "::error::Generated code is out of date. Run 'make proto' and commit the changes."
            exit 1
          fi

  osv:
    name: OSV Scan
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install osv-scanner
        run: brew install osv-scanner
      - name: Run OSV scan
        run: make osv