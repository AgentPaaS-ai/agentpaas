version: 2

project_name: agentpaas

before:
  hooks:
    - go mod tidy

builds:
  - id: agentpaas
    main: ./cmd/agent
    binary: agentpaas
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/AgentPaaS-ai/agentpaas/internal/daemon.CLIVersion={{.Version}}
      - -X github.com/AgentPaaS-ai/agentpaas/internal/daemon.DaemonVersion={{.Version}}
      - -X github.com/AgentPaaS-ai/agentpaas/internal/daemon.GitCommit={{.Commit}}

  - id: agentpaasd
    main: ./cmd/agentpaasd
    binary: agentpaasd
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/AgentPaaS-ai/agentpaas/internal/daemon.CLIVersion={{.Version}}
      - -X github.com/AgentPaaS-ai/agentpaas/internal/daemon.DaemonVersion={{.Version}}
      - -X github.com/AgentPaaS-ai/agentpaas/internal/daemon.GitCommit={{.Commit}}

  - id: agentpaas-harness-linux
    main: ./cmd/harness
    binary: agentpaas-harness-linux
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/AgentPaaS-ai/agentpaas/internal/harness.HarnessVersion={{.Version}}-{{.ShortCommit}}

  - id: agentpaas-harness-linux-amd64
    main: ./cmd/harness
    binary: agentpaas-harness-linux-amd64
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/AgentPaaS-ai/agentpaas/internal/harness.HarnessVersion={{.Version}}-{{.ShortCommit}}

archives:
  - id: default
    ids:
      - agentpaas
      - agentpaasd
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    files:
      - LICENSE
      - README.md
      - demo/**/*
      - src: dist/agentpaas-harness-linux_linux_arm64_v8.0/agentpaas-harness-linux
        dst: agentpaas-harness-linux
      - src: dist/agentpaas-harness-linux-amd64_linux_amd64_v1/agentpaas-harness-linux-amd64
        dst: agentpaas-harness-linux-amd64

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

sboms:
  - id: default
    artifacts: archive

signs:
  - id: cosign
    cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - sign-blob
      - "--bundle=${signature}"
      - "--yes"
      - "${artifact}"
    artifacts: checksum

homebrew_casks:
  - name: agentpaas
    homepage: https://github.com/AgentPaaS-ai/agentpaas
    description: "Governed, local-first runtime for AI-generated agents"
    binaries:
      - agentpaas
      - agentpaasd
      - agentpaas-harness-linux
      - agentpaas-harness-linux-amd64
    repository:
      owner: AgentPaaS-ai
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: github-actions[bot]
      email: 41898282+github-actions[bot]@users.noreply.github.com

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'

release:
  github:
    owner: AgentPaaS-ai
    name: agentpaas
  prerelease: auto
  name_template: "v{{.Version}}"
