name: weather-agent
version: 0.1.0
runtime: python3.12
entry: main.py

llm:
  provider: openrouter  # openrouter|openai|anthropic|xai|nous
  model: deepseek/deepseek-v4-flash
  credential: openrouter-key  # Keychain secret name
